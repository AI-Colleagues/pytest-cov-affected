[bumpversion]
current_version = 0.0.1
commit = True
tag = True

[bumpversion:file:pyproject.toml]
search = version = "{current_version}"
replace = version = "{new_version}"

[bumpversion:file:uv.lock]
search = [[package]]
	name = "pytest-cov-affected"
	version = "{current_version}"
replace = [[package]]
	name = "pytest-cov-affected"
	version = "{new_version}"
